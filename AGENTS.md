# Agent Instructions

- Communicate with the user in Traditional Chinese (zh-TW).
- Use English for internal reasoning, planning, command execution, and implementation work unless a task explicitly requires another language.
- Do not read or modify `CLAUDE.md`.
- Do not rerun `led_off.sh` or any LED/WMI command after the LEDs are confirmed off, unless the user explicitly asks for it.
